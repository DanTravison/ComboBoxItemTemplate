<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:objectmodel="clr-namespace:ObjectModel"
             xmlns:viewmodels="clr-namespace:ComboBoxItemTemplate.ViewModels"
             xmlns:input="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             xmlns:views="clr-namespace:ComboBoxItemTemplate.Views"
             xmlns:sf="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             x:Class="ComboBoxItemTemplate.Views.MainPage"
             x:DataType="viewmodels:MainViewModel">

    <ContentPage.Resources>
        <Style TargetType="views:ComboBox" x:Key="ComboBoxStyle">
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="WidthRequest" Value="200"/>
            <Setter Property="IsEditable" Value="False"/>
            <Setter Property="IsClearButtonVisible" Value="False"/>
            <Setter Property="DropDownBackground" Value="{StaticResource PopupBackgroundColor}"/>
            <Setter Property="ItemTemplate" Value="{StaticResource CommandTemplateSelector}"/>
            <Setter Property="ItemsSource" Value="{Binding Commands, Mode=OneWay}"/>
            <Setter Property="SelectedItem" Value="{Binding SelectedCommand, Mode=TwoWay}"/>
        </Style>
    </ContentPage.Resources>

    <Grid ColumnDefinitions="Auto, Auto, Auto"
          RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto, *"
          HorizontalOptions="Center"
          VerticalOptions="Center"
          ColumnSpacing="10"
          >
        <Label Grid.Row="0"
               Grid.Column="0" 
               HorizontalTextAlignment="End"
               VerticalTextAlignment="Center"
               Text="Select An Item"/>

        <views:ComboBox Grid.Row="0"
                       Grid.Column="1"
                       Style="{StaticResource ComboBoxStyle}"
                       />
         <Button Command="{Binding RefreshCommand}"
                 IsEnabled="true"
                 Grid.Column="2"
                 Grid.Row="0"
                 Text="{Binding RefreshCommand.Text}"
                 ToolTipProperties.Text="{Binding RefreshCommand.Description}"
                 />

        <Label Grid.Row="1"
               Grid.Column="0" 
               HorizontalTextAlignment="End"
               VerticalTextAlignment="Center"
               Text="Header + Footer"/>

        <views:ComboBox Grid.Row="1"
                       Grid.Column="1"
                       Style="{StaticResource ComboBoxStyle}"
                       ShowDropdownHeaderView="True"
                       ShowDropdownFooterView="True"
                       >
            <views:ComboBox.DropdownHeaderView>
                <Label Text="Header Text"
                       TextColor="Black"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center"
                       FontAttributes="Bold"
                       FontSize="16"
                       />
            </views:ComboBox.DropdownHeaderView>
            <views:ComboBox.DropdownFooterView>
                <Label Text="Footer Text"
                       TextColor="Black"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center"
                       FontSize="24"
                       />
                </views:ComboBox.DropdownFooterView>
            </views:ComboBox>

        <Label Grid.Column="0" 
               Grid.Row="2"
               HorizontalTextAlignment="End"
               VerticalTextAlignment="Center"
               Text="Header"/>

        <views:ComboBox Grid.Column="1"
                       Grid.Row="2"
                       Style="{StaticResource ComboBoxStyle}"
                       ShowDropdownHeaderView="True"
                       >
            <views:ComboBox.DropdownHeaderView>
                <Label Text="Header Text"
                       TextColor="Black"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center"
                       FontAttributes="Bold"
                       FontSize="16"
                       />
            </views:ComboBox.DropdownHeaderView>
        </views:ComboBox>

        <Label Grid.Column="0" 
               Grid.Row="3"
               HorizontalTextAlignment="End"
               VerticalTextAlignment="Center"
               Text="Footer"/>

        <views:ComboBox Grid.Column="1"
                      Grid.Row="3"
                      Style="{StaticResource ComboBoxStyle}"
                      ShowDropdownFooterView="True"
                      >
            <views:ComboBox.DropdownFooterView>
                <Label Text="Footer Text"
                       TextColor="Black"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center"
                       FontSize="24"
                       />
            </views:ComboBox.DropdownFooterView>
        </views:ComboBox>

    </Grid>

</ContentPage>
